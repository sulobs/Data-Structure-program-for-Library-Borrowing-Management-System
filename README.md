# Data-Structure-program-for-Library-Borrowing-Management-System
Data Structure program for Library Borrowing Management System: Library borrowing management program efficiently computes average borrowings, identifies highest and lowest borrowed books, counts inactive members, and determines the mode of borrow counts with an overall time complexity of O(n + m) and space complexity of O(n).
